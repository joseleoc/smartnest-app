import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    litItem: { flexDirection: "row", alignItems: "center", },
    itemIcon: {
        borderRadius: 100,
        display: "flex",
        alignContent: "center",
        justifyContent: "center",
    }
});